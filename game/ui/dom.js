export function assignGameUiDomRefs(ui) {
    ui.header = document.querySelector('header');
    ui.hudBar = document.querySelector('.hud-bar');
    ui.hudLapCluster = document.querySelector('.hud-lap-cluster');
    ui.hudStatsBtn = document.getElementById('hud-stats-btn');
    ui.timeVal = document.getElementById('time-val');
    ui.timeDisplay = ui.timeVal?.closest('.hud-stat') || null;
    ui.timeLabel = ui.timeDisplay?.querySelector('.hud-label') || null;
    ui.speedVal = document.getElementById('speed-val');
    ui.desktopSpeedometer = document.getElementById('desktop-speedometer');
    ui.desktopPracticePauseBtn = document.getElementById('desktop-practice-pause-btn');
    ui.mobileSpeedometer = document.getElementById('mobile-speedometer');
    ui.mobileSpeedVal = document.getElementById('mobile-speed-val');
    ui.mobilePracticePauseBtn = document.getElementById('mobile-practice-pause-btn');
    ui.bestTimeDisplay = document.getElementById('best-time-display');
    ui.bestTimeDivider = document.getElementById('best-time-divider');
    ui.bestTimeLabel = document.getElementById('best-time-label');
    ui.bestTimeVal = document.getElementById('best-time-val');
    ui.modal = document.getElementById('modal');
    ui.modalTitle = document.getElementById('modal-title');
    ui.modalMsg = document.getElementById('modal-msg');
    ui.modalLapTimes = document.getElementById('modal-lap-times');
    ui.modalStatsRow = document.getElementById('modal-stats-row');
    ui.modalPreviewWrap = document.getElementById('modal-preview-wrap');
    ui.modalPreviewImg = document.getElementById('modal-preview-img');
    ui.backToMainBtn = document.getElementById('back-to-main-btn');
    ui.modalMainView = document.getElementById('modal-main-view');
    ui.modalRunsView = document.getElementById('modal-runs-view');
    ui.shareBtn = document.getElementById('share-btn');
    ui.modalSecondaryBtn = document.getElementById('modal-secondary-btn');
    ui.startOverlay = document.getElementById('start-overlay');
    ui.startGroup = document.getElementById('start-group');
    ui.firstTimeMsg = document.getElementById('first-time-msg');
    ui.modeSelectionPanel = document.getElementById('mode-selection-panel');
    ui.modeSelectStandardBtn = document.getElementById('mode-select-standard-btn');
    ui.modeSelectPracticeBtn = document.getElementById('mode-select-practice-btn');
    ui.modeSelectDailyBtn = document.getElementById('mode-select-daily-btn');
    ui.modeSelectDailyTitleLabel = document.querySelector('#mode-select-daily-btn .mode-selection-row__title-label');
    ui.modeSelectDailyCopy = document.getElementById('mode-select-daily-copy');
    ui.returningPlayerPanel = document.getElementById('returning-player-panel');
    ui.returningPlayerHeading = document.getElementById('returning-player-heading');
    ui.changeTrackModeBtn = document.getElementById('change-track-mode-btn');
    ui.trackCountIndicator = document.getElementById('track-count-indicator');
    ui.trackSelectorFrame = document.querySelector('.track-selector-frame');
    ui.trackCarouselShell = document.querySelector('.track-carousel-shell');
    ui.trackCarousel = document.getElementById('track-carousel');
    ui.trackPrevBtn = document.getElementById('track-prev-btn');
    ui.trackNextBtn = document.getElementById('track-next-btn');
    ui.returningStartBtn = document.getElementById('returning-start-btn');
    ui.scoreModeIntro = document.getElementById('score-mode-intro');
    ui.scoreModeIntroDismiss = document.getElementById('score-mode-intro-dismiss');
    ui.dailyChallengePanel = document.getElementById('daily-challenge-panel');
    ui.dailyChallengeTitle = document.getElementById('daily-challenge-title');
    ui.dailyChallengeTrack = document.getElementById('daily-challenge-track');
    ui.dailyChallengeObjective = document.getElementById('daily-challenge-objective');
    ui.dailyChallengeModifiers = document.getElementById('daily-challenge-modifiers');
    ui.dailyChallengeBestLabel = document.getElementById('daily-challenge-best-label');
    ui.dailyChallengeBest = document.getElementById('daily-challenge-best');
    ui.dailyChallengeRankBtn = document.getElementById('daily-challenge-rank-btn');
    ui.dailyChallengeRank = document.getElementById('daily-challenge-rank');
    ui.dailyChallengeReset = document.getElementById('daily-challenge-reset');
    ui.dailyChallengeStartBtn = document.getElementById('daily-challenge-start-btn');
    ui.dailyChallengeBackBtn = document.getElementById('daily-challenge-back-btn');
    ui.dailyChallengePreviewCanvas = document.getElementById('daily-challenge-preview');
    ui.startLights = document.getElementById('start-lights');
    ui.goMessage = document.getElementById('go-message');
    ui.practiceLapFlash = document.getElementById('practice-lap-flash');
    ui.practiceLapFlashLabel = document.getElementById('practice-lap-flash-label');
    ui.practiceLapFlashTime = document.getElementById('practice-lap-flash-time');
    ui.practiceLapFlashDelta = document.getElementById('practice-lap-flash-delta');
    ui.htpModal = document.getElementById('how-to-play-modal');
    ui.closeHtpBtn = document.getElementById('close-htp-btn');
    ui.closeHtpXBtn = document.getElementById('htp-modal-close');
    ui.headerHtpBtn = document.getElementById('header-htp-btn');
    ui.headerNavMenu = document.getElementById('header-nav-menu');
    ui.menuHowToPlayBtn = document.getElementById('menu-how-to-play');
    ui.startBtn = document.getElementById('start-btn');
    ui.modalResetBtn = document.getElementById('modal-reset-btn');
    ui.leftTouchBtn = document.getElementById('btn-left');
    ui.rightTouchBtn = document.getElementById('btn-right');
    ui.dailyChallengeHudInline = document.getElementById('daily-challenge-hud-inline');
    ui.countdownLights = [
        document.getElementById('light-1'),
        document.getElementById('light-2'),
        document.getElementById('light-3')
    ];
}
